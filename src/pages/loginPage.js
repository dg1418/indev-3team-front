/**
 * @file loginPage.js
 * @description 사용자 로그인 기능을 제공하는 페이지 컴포넌트입니다.
 *              사용자 인증(로그인) UI 및 관련 로직을 포함합니다.
 */
import React from 'react';

const LoginPage = () => {
  return (
    <div>
      <h1>로그인</h1>
      {/* 로그인 폼이 여기에 들어갑니다. */}
    </div>
  );
};

export default LoginPage;
